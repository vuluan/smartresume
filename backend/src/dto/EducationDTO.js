export class EducationDTO {
    constructor(user_id, school, degree, field, start, finish) {
        this.user_id = user_id;
        this.school = school;
        this.degree = degree;
        this.field = field;
        this.start = start;
        this.finish = finish;
    }
}